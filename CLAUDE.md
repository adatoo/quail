Read AGENTS.md first — it holds the conventions, build commands and definition of done for this repo.

Then, for any feature work: docs/ARCHITECTURE.md (design), docs/IMPLEMENTATION_PLAN.md (what's next, in order), docs/DECISIONS.md (why things are the way they are — add a D-nnn entry when you change a decision).

Start with the "First session with Claude Code" list at the end of docs/IMPLEMENTATION_PLAN.md.
