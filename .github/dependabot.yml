version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    # Conventional Commits titles ("chore(deps): bump …"), so the PR's
    # version bump can be derived from its title (ADR D-024).
    commit-message:
      prefix: "chore"
      include: "scope"
